{
  "name": "srs-core",
  "version": "0.1.0",
  "private": true,
  "description": "Headless spaced-repetition core (FSRS) for the study site. Browser IndexedDB + in-memory store.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ts-fsrs": "^4.7.0"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
